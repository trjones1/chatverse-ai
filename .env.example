OPENAI_API_KEY=<SUPER_SECRET>
ELEVENLABS_API_KEY=<SUPER_SECRET>
VOICE_ID_LEXI=<SUPER_SECRET>
VOICE_ID_NYX=<SUPER_SECRET>
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=<SUPER_SECRET>
STRIPE_SECRET_KEY=<SUPER_SECRET>
NEXT_PUBLIC_SUPABASE_URL=<SUPER_SECRET>
NEXT_PUBLIC_SUPABASE_PUBLIC_KEY=<SUPER_SECRET>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<SUPER_SECRET>
SUPABASE_SERVICE_ROLE_KEY=<SUPER_SECRET>
OPENROUTER_API_KEY=<SUPER_SECRET>
NEXT_PUBLIC_GTM_ID=<SUPER_SECRET>
STRIPE_WEBHOOK_SECRET=<SUPER_SECRET>
RESEND_API_KEY=<SUPER_SECRET>

# Gumroad Integration
GUMROAD_ACCESS_TOKEN=<SUPER_SECRET>
GUMROAD_PING_TOKEN=<SUPER_SECRET>  # Random secret for webhook auth (e.g., generate with: openssl rand -base64 32)

# Female Character Pricing (Lexi, Nyx, Chloe, Aiko, Zaria, Nova)
F_STRIPE_PRICE_SFW=<SUPER_SECRET>
F_STRIPE_PRICE_NSFW=<SUPER_SECRET>
F_STRIPE_PRICE_VOICE_10=<SUPER_SECRET>
F_STRIPE_PRICE_VOICE_25=<SUPER_SECRET>
F_STRIPE_PRICE_VOICE_50=<SUPER_SECRET>
F_STRIPE_PRICE_VOICE_100=<SUPER_SECRET>

# Male Character Pricing (Dom, Dominic, Chase, Ethan, Jayden, Miles)
M_STRIPE_PRICE_SFW=<SUPER_SECRET>
M_STRIPE_PRICE_NSFW=<SUPER_SECRET>
M_STRIPE_PRICE_VOICE_10=<SUPER_SECRET>
M_STRIPE_PRICE_VOICE_25=<SUPER_SECRET>
M_STRIPE_PRICE_VOICE_50=<SUPER_SECRET>
M_STRIPE_PRICE_VOICE_100=<SUPER_SECRET>

# Premium Character Pricing (Dom, Nyx) - Higher tier for premium characters
DOM_STRIPE_PRICE_NSFW_PREMIUM=<SUPER_SECRET>
NYX_STRIPE_PRICE_NSFW_PREMIUM=<SUPER_SECRET>
NEXT_PUBLIC_SITE_URL=http://localhost:3000/
DEBUG_TOKEN=dev
PUBLIC_SITE_URL=http://localhost:3000
NODE_ENV=not-production
CHARACTER_KEY=lexi

# Debug Flags (Development Only)
# Set these to 'true' to enable detailed logging for debugging
# Set to 'false' or remove entirely for production to reduce log noise
DEBUG_MEMORY=true        # Memory system debugging (message loading, user IDs, etc.)
DEBUG_CHAT=true          # Chat API debugging (AI messages, conversation history)
DEBUG_AUTH=true          # Authentication debugging (login, tokens, etc.)

# Admin Tools (Development Only)
# Enable admin testing tools and cleanup APIs - DO NOT enable in production
NEXT_PUBLIC_ENABLE_ADMIN_TOOLS=true  # Shows admin UI components
ENABLE_ADMIN_TOOLS=true              # Enables admin API endpoints

# Sentry Error Monitoring
# Get these values from your Sentry project settings
NEXT_PUBLIC_SENTRY_DSN=<SUPER_SECRET>
SENTRY_ORG=<YOUR_SENTRY_ORG>
SENTRY_PROJECT=<YOUR_SENTRY_PROJECT>
SENTRY_AUTH_TOKEN=<SUPER_SECRET>

# Sentry Debug Flags (Development Only)
SENTRY_DEBUG=false               # Enable Sentry debug logging in development
NEXT_PUBLIC_SENTRY_DEBUG=false   # Enable client-side Sentry in development

# Production deployment should set:
# DEBUG_MEMORY=false
# DEBUG_CHAT=false  
# DEBUG_AUTH=false
# NEXT_PUBLIC_ENABLE_ADMIN_TOOLS=false (or omit)
# ENABLE_ADMIN_TOOLS=false (or omit)
# NODE_ENV=production (safe to set without breaking functionality)
# SENTRY_DEBUG=false (or omit)
# NEXT_PUBLIC_SENTRY_DEBUG=false (or omit)